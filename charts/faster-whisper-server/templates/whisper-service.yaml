apiVersion: v1
kind: Service
metadata:
  name: {{ include "faster-whisper-server.fullname" . }}
  labels:
    {{- include "faster-whisper-server.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.fasterWhisper.port }}
      targetPort: whisper-http
      protocol: TCP
      name: whisper-http
      # Tells anything that reads it — a service mesh, some ingress
      # controllers — that the backend speaks TLS. ingress-nginx does NOT read
      # this; it wants the backend-protocol annotation on the Ingress instead.
      appProtocol: {{ if .Values.tls.enabled }}https{{ else }}http{{ end }}
  selector:
    {{- include "faster-whisper-server.selectorLabels" . | nindent 4 }}
